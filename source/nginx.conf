worker_processes 1;

events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name localhost; /* 192.168.1.X and other local IP */

        location / {
            root /etc/nginx;
            index index.html;
        }
    }
}
